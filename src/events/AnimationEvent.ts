export const enum AnimationEvent {    
    START = "__animation_start",
    UPDATE = "__animation_update",
    STOP = "__animation_stop",    
    PAUSE = "__animation_pause",    
    RESUME = "__animation_resume",
}