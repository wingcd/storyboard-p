export const enum TimelineEvent {    
    START = "__timeline_start",
    UPDATE = "__timeline_update",
    STOP = "__timeline_stop",    
    PAUSE = "__timeline_pause",    
    RESUME = "__timeline_resume",
}